import Profile from "@/components/adminDashboard/profile/Profile";

export const metadata = {
  title: "Account Settings | HiChandra Admin",
  description: "Manage your admin profile, change password, and configure platform settings.",
};

export default function Page() {
  return <Profile />;
}
