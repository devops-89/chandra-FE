import HeroSection from '@/components/common/hero/HeroSection';

const Page = () => {
  return (
    <main className="min-h-screen bg-white">
      <HeroSection />
    </main>
  );
};

export default Page;
