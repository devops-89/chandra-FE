export type HeroContentConfig = {
  label: string;
  heading: string;
  headingHighlight: string;
  description: string;
  cta: {
    label: string;
    href: string;
  };
  image: {
    src: string;
    alt: string;
  };
};
